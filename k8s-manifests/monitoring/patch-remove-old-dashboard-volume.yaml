apiVersion: apps/v1
kind: Deployment
metadata:
  name: kube-prometheus-stack-grafana
  namespace: monitoring
spec:
  template:
    spec:
      volumes:
        - name: dashboards
          $patch: delete
      containers:
        - name: grafana
          volumeMounts:
            - name: dashboards
              $patch: delete
