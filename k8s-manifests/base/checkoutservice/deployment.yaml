# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: checkoutservice
#   namespace: checkoutservice
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: checkoutservice
#   template:
#     metadata:
#       labels:
#         app: checkoutservice
#     spec:
#       serviceAccountName: ecr-access-sa
#       containers:
#         - name: checkoutservice
#           image: checkoutservice:latest
#           ports:
#             - containerPort: 5050

apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkoutservice
  namespace: checkoutservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: checkoutservice
  template:
    metadata:
      labels:
        app: checkoutservice
    spec:
      serviceAccountName: ecr-access-sa
      containers:
        - name: checkoutservice
          image: checkoutservice:latest   # ArgoCD/Kustomize will patch this automatically
          ports:
            - containerPort: 5050
          env:
            - name: PORT
              value: "5050"

            - name: SHIPPING_SERVICE_ADDR
              value: "shippingservice.shippingservice.svc.cluster.local:8080"

            # --- OPTIONAL but common in this microservices demo ---
            - name: CART_SERVICE_ADDR
              value: "cartservice.cartservice.svc.cluster.local:7070"
            - name: CURRENCY_SERVICE_ADDR
              value: "currencyservice.currencyservice.svc.cluster.local:7000"
            - name: EMAIL_SERVICE_ADDR
              value: "emailservice.emailservice.svc.cluster.local:8080"
